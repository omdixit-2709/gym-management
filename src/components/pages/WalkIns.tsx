import React from 'react';
import { Box, Typography } from '@mui/material';

const WalkIns: React.FC = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Walk-ins</Typography>
      <Typography variant="body1">Manage walk-in visitors here</Typography>
    </Box>
  );
};

export default WalkIns; 